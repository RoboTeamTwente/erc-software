services:
  ros-desktop:
    image: osrf/ros:kilted-desktop-full
    command: [gz, sim]
    environment:
      DISPLAY: ${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    runtime: nvidia
    gpus: all
